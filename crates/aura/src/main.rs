//! aurad: Aura's daemon. Supervises llama-server, whisper.cpp and piper, exposes dev.eclipse.Aura on D-Bus and an OpenAI-style API on localhost. Nothing here yet.

fn main() {
    println!("aurad {} (stub)", libeclipse::VERSION);
}
