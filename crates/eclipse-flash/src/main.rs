//! eclipse-flash: writes Eclipse to a stick from macOS, Windows or Linux. Refuses anything that looks like an internal disk. Nothing here yet.

fn main() {
    println!("eclipse-flash {} (stub)", libeclipse::VERSION);
}
