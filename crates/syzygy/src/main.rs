//! syzygy: runs before the session. Fingerprints the host, loads its profile, picks the GPU path and the AI tier. Nothing here yet.

fn main() {
    println!("syzygy {} (stub)", libeclipse::VERSION);
}
